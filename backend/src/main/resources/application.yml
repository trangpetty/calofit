spring:
  application:
    name: 'calofit-app'
  datasource:
    url: jdbc:mysql://mysql-29df473e-trangpetty22072001-7d7d.b.aivencloud.com:13466/calofit_db?sslMode=REQUIRED&allowPublicKeyRetrieval=true
    username: avnadmin
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  data:
    redis:
      host: localhost
      port: 6379

app:
  google:
    client-id: 978787902078-k4ghrb012l8pmkj9ts01274s62a7stuo.apps.googleusercontent.com