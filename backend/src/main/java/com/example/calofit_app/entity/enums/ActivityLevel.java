package com.example.calofit_app.entity.enums;

public enum ActivityLevel {
    SEDENTARY,      // Ít vận động (làm việc văn phòng)
    LIGHTLY_ACTIVE, // Vận động nhẹ (tập luyện 1-3 ngày/tuần)
    MODERATELY_ACTIVE, // Vận động vừa (3-5 ngày/tuần)
    VERY_ACTIVE,    // Vận động nhiều (6-7 ngày/tuần)
    EXTRA_ACTIVE    // Vận động cường độ cao (vận động viên)
}
